services:
  postgres:
    container_name: postgres_db
    image: postgres
    environment:
      POSTGRES_USER: threds_user
      POSTGRES_PASSWORD: threds
      POSTGRES_DB: threds
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql


volumes:
  postgres_data: